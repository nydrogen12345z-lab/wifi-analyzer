name: wifi_analyzer
description: WiFi Analyzer and Network Manager
version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  network_info_plus: ^4.0.2
  wifi_iot: ^0.3.18
  permission_handler: ^10.2.0
  connectivity_plus: ^4.0.2
  shared_preferences: ^2.1.1
  fl_chart: ^0.62.0
  provider: ^6.0.5
  google_fonts: ^4.0.4
  url_launcher: ^6.1.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
